import { SearchByPipe } from './search-by-name.pipe';

describe('SearchByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByPipe();
    expect(pipe).toBeTruthy();
  });
});
